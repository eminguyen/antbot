<div class="ui text container">
  <div class="ui celled list">
    {{#each commands}}
        <div class="item">
          <div class="content">
            <div class="header">{{usage}}</div>
            {{description}}
          </div>
        </div>
    {{/each}}
  </div>
</div>
